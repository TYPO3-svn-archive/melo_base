plugin.tx_jppageteaser_pi1 {
    
	// Define the paths leading to our HTML template files
#    templatePath = {$plugin.tx_jppageteaser_pi1.templatePath}
    
    // Define the filenames used as the default HTML templates
#    defaultTemplateFileName = {$plugin.tx_jppageteaser_pi1.defaultTemplateFileName}
	
    marker {
	    IMAGE {
    		file.maxW = 200
		}
	}
}